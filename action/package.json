{
  "name": "movie-action",
  "version": "1.0.0",
  "description": "A GitHub Action to write content to a file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  }
}
